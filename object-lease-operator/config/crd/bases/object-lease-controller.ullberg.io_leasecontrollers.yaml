---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: leasecontrollers.object-lease-controller.ullberg.io
spec:
  group: object-lease-controller.ullberg.io
  names:
    kind: LeaseController
    listKind: LeaseControllerList
    plural: leasecontrollers
    singular: leasecontroller
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: LeaseController is the Schema for the leasecontrollers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of LeaseController
            type: object
            required:
              - version
              - kind
            x-kubernetes-preserve-unknown-fields: true
            properties:
              group:
                description: Group defines the group of the object the LeaseController is managing.
                type: string
              version:
                description: Version defines the version of the object the LeaseController is managing.
                type: string
              kind:
                description: Kind defines the kind of the object the LeaseController is managing.
                type: object
                required:
                  - singular
                  - plural
                properties:
                  singular:
                    description: Singular is the singular name of the kind.
                    type: string
                  plural:
                    description: Plural is the plural name of the kind.
                    type: string
              webhook:
                description: Webhook defines the validation webhook configuration for this GVK.
                type: object
                properties:
                  enabled:
                    description: Enabled determines whether validation is enabled for this GVK.
                    type: boolean
                    default: false
                  failurePolicy:
                    description: FailurePolicy defines how unrecognized errors are handled. Can be "Fail" (reject invalid objects) or "Ignore" (log but allow).
                    type: string
                    enum:
                      - Fail
                      - Ignore
                    default: Ignore
          status:
            description: Status defines the observed state of LeaseController
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
